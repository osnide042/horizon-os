# Horizon OS keeps the destructive disk choice visible, but answers the
# repetitive Debian questions automatically.
d-i auto-install/enable boolean true
d-i debian-installer/locale string en_GB.UTF-8
d-i keyboard-configuration/xkb-keymap select gb
d-i keyboard-configuration/layoutcode string gb

d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string horizon-os
d-i netcfg/hostname string horizon-os
d-i netcfg/get_domain string local
d-i netcfg/wireless_wep string

d-i mirror/country string manual
d-i mirror/http/hostname string deb.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string
d-i mirror/suite string trixie

d-i passwd/root-login boolean false
d-i clock-setup/utc boolean true
d-i time/zone string Europe/London
d-i clock-setup/ntp boolean true
d-i pkgsel/upgrade select safe-upgrade
popularity-contest popularity-contest/participate boolean false

# A user still explicitly chooses the target disk and confirms formatting.
# Guided partitioning then uses one simple partition layout.
d-i partman-auto/method string regular
d-i partman-auto/choose_recipe select atomic
d-i partman-lvm/device_remove_lvm boolean true
d-i partman-md/device_remove_md boolean true

d-i grub-installer/only_debian boolean true
d-i grub-installer/with_other_os boolean true
d-i finish-install/reboot_in_progress note
